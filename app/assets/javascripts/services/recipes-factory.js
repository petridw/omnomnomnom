angular
  .module('goodbelly')
  .factory('RecipesList', [
    function() {
      var RecipesList = function(keywords, ingredients) {
        this.initialize = function() {
          
        };

        this.initialize();
      };

      return (RecipesList);
    }
  ]);
